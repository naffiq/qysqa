language: php

php:
  - 5.6
  - 7.0

before_script:
  - cp .env.travis .env
  - mysql -e 'create database db_qysqa_test;'
  - composer self-update
  - composer install --no-interaction
  - php artisan migrate

script:
  - vendor/bin/phpunit
